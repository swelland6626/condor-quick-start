######################################
Using Docker
######################################

   .. code-block:: bash

      # download existing docker image from docker repo

      # build docker image

      # run docker image
